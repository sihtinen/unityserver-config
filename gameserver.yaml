apiVersion: "agones.dev/v1"
kind: GameServer
metadata:
  generateName: "unityserver"
spec:
  ports:
  - name: default
    portPolicy: Dynamic
    containerPort: 7776
  template:
    spec:
      containers:
      - name: unityserver
        image: sihtinen/unityserver
      resources:
        requests:
          memory: "128Mi"
          cpu: "128m"
        limits:
          memory: "128Mi"
          cpu: "128m"
